# Core Model Types & Zod Schemas

## Question

Port all 12 domain models from docs/02-data-models-and-schema.md into `src/core/models/` as TypeScript types with branded types and Zod validation schemas.

This is a translation task — the models are fully specified. Includes: Transaction, Account, Category, Tag, Budget, Loan, LoanRecord, PlannedPaymentRule, ExchangeRate, Value types, and branded primitives (PositiveDouble, NotBlankTrimmedString, etc.).
