# Budget: Data Layer + Store

## Question

Build the Budget data layer (schema, repo interface, impl, store, unit tests).

Drizzle schema: budgets table with categoryIds and accountIds stored as serialized JSON arrays.

## Dependencies

- Blocked by #8 (Account store)
- Blocked by #11 (Category data layer)
