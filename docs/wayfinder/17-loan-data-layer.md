# Loan + LoanRecord: Data Layer + Store

## Question

Build the Loan and LoanRecord data layer (schema, repo interfaces, impls, store, unit tests).

Two related entities — Loan and LoanRecord share a store per the original Ivy pattern. Loan has FK to Account.

## Dependencies

- Blocked by #8 (Account store — Loan.accountId)
