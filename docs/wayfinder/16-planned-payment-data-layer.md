# Planned Payment Rule: Data Layer + Store

## Question

Build the PlannedPaymentRule data layer (schema, repo interface, impl, store, unit tests).

Drizzle schema: planned_payment_rules table. Repo methods include interval-based queries for upcoming/overdue calculations.

## Dependencies

- Blocked by #8 (Account store)
- Blocked by #11 (Category data layer)
