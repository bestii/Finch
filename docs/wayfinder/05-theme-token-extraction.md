# Theme Token Extraction

## Question

Extract design tokens (colors, typography, spacing, border radii) from the Figma design system into `src/core/theme/` as a shared token file that both mobile (react-native-paper) and web (TBD) can consume.

The Figma link is in the docs: https://www.figma.com/file/kSwIa07jcHEHZXo6rzx7dn/Design-System

This is research — we need to inspect the Figma file and surface the token structure.
