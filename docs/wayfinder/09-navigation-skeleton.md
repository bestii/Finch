# Navigation Skeleton + MainScreen

## Question

Create the React Navigation shell with all user-facing routes registered as empty placeholder screens:

- `src/mobile/navigation/` — NavigationContainer, Stack Navigator, Bottom Tab Navigator
- MainScreen: 2-tab layout (Home + Accounts) + FAB that expands into quick actions
- 15 empty placeholder screens with registered routes and typed params
- Each screen renders its name as placeholder text (no data, no components)

**Screen list**: Home, Accounts, EditTransaction, Transactions, Categories, PlannedPayments, EditPlanned, PieChart, Report, Budget, Balance, ExchangeRates, Loans, LoanDetails, Search, Settings

## Dependencies

- Blocked by #6 (Theme tokens — nav skeleton needs the theme for the navigation bars)
